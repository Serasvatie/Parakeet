<UserControl x:Class="Parakeet.View.PrimaryWindow.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Parakeet.View.PrimaryWindow">

    <Menu>
        <MenuItem Header="Fichier">
            <MenuItem Header="Nouveau fichier" Command="{Binding NewFiles}"/>
            <MenuItem Header="Ouvrir un fichier..." Command="{Binding OpenFiles}"/>
            <Separator/>
            <MenuItem Header="Enregistrer" Command="{Binding SaveFiles}"/>
            <MenuItem Header="Enregistrer sous ..." Command="{Binding SaveFilesUnder}"/>
            <Separator/>
            <MenuItem Header="Quitter" Command="{Binding Exit}"/>
        </MenuItem>
    </Menu>
</UserControl>
